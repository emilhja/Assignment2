@emil-hjaertfors-agent, @emil-flyghed-agent @hassan-swe-agent @alexia-kazim-agent, @sonia-agent, @rebecka-vannerberg
You are going to act as a 4-agent development team working together to build a project. @alexia-kazim-agent takes the planner role, @hassan-swe-agent takes the developer role, emil-hjaertfors-agent takes the bug tester role and emil-flyghed-agent takes the final refiner role . 

Do not stop until all 4 agents have completed their tasks.  The 4 Agents and their strict workflow are:  
1. The Product Planner: Defines the scope. Lists every single feature, input, button, and expected output. Keeps the project realistic and simple. 
2. The 2 Developers: Takes the Planner's scope and writes the complete, raw code. No shortcuts, no placeholders (like "// insert code here"), and no half-finished logic. Write it out fully. 
3. The Bug Tester: Simulates running the Developer's code. Looks for edge cases, missing edge-case logic (like dividing by zero, empty inputs, or letters typed into number fields), and lists any flaws. 
4. The Final Refiner: Takes the Tester's feedback, applies the necessary bug fixes to the code, and outputs the final, perfected version of the code for the user.  
5. Documenter: Your role is to document

Project to build : A simple calculator in python with +, -, * and / with a terminal interface


@emil-hjaertfors-agent, @emil-flyghed-agent @alexia-kazim-agent
You are going to act as a 3-agent development team working together to build a project. alexia-kazim-agent takes the planner role, emil-flyghed-agent takes the developer role, emil-hjaertfors-agent takes the bug tester role and alexia-kazim-agent takes the final refiner role . Do not stop until all 4 agents have completed their tasks.  The 4 Agents and their strict workflow are:  
1. The Product Planner: Defines the scope. Lists every single feature, input, button, and expected output. Keeps the project realistic and simple. 
2. The Lead Developer: Takes the Planner's scope and writes the complete, raw code. No shortcuts, no placeholders (like "// insert code here"), and no half-finished logic. Write it out fully. 
3. The Bug Tester: Simulates running the Developer's code. Looks for edge cases, missing edge-case logic (like dividing by zero, empty inputs, or letters typed into number fields), and lists any flaws. 
4. The Final Refiner: Takes the Tester's feedback, applies the necessary bug fixes to the code, and outputs the final, perfected version of the code for the user.  

Project to build : A simple calculator in python with +, -, * and / with a terminal



@emil-hjaertfors-agent, @emil-flyghed-agent @hassan-swe-agent @alexia-kazim-agent, @sonia-agent, @rebecka-vannerberg
You are going to act as a agent development team working together to build a project. 

Do not stop until all agents have completed their tasks.  The agents and their strict workflow are:  
1. The Product Planner: Defines the scope. Lists every single feature, input, button, and expected output. Keeps the project realistic and simple. 
2. The 2 developers to divide the work before starting coding: Takes the Planner's scope and writes the complete, raw code. No shortcuts, no placeholders (like "// insert code here"), and no half-finished logic. Write it out fully. 
3. The Bug Tester: Simulates running the Developer's code. Looks for edge cases, missing edge-case logic (like dividing by zero, empty inputs, or letters typed into number fields), and lists any flaws. 
4. The Final Refiner: Takes the Tester's feedback, applies the necessary bug fixes to the code, and outputs the final, perfected version of the code for the user.  
5. Documenter: Your role is to document

Project to build : A simple fizzbuzz game in python with +, -, * and / with a terminal interface


Project to build : A simple snake game in the web browser