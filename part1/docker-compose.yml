services:
  agent:
    build: .
    env_file:
      - .env
    stdin_open: true
    tty: true
    working_dir: /workspace
    command: ["python", "/app/agent.py"]
    volumes:
      - ./workspace:/workspace
    cpus: "1.0"
    mem_limit: 512m
